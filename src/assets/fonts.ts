import { Onest, Inconsolata } from "next/font/google";

export const onest = Onest({
  subsets: ["latin"],
  display: "swap",
  weight: ["400", "700"],
});

export const inconsolata = Inconsolata({
  subsets: ["latin"],
  display: "swap",
  weight: ["400", "700"],
});